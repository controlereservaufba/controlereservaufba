<%= form_with(model: controle, local: true) do |form| %>
  <% if controle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(controle.errors.count, "error") %> prohibited this controle from being saved:</h2>

      <ul>
      <% controle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
<table>
    <tr>
     <td><%= form.label :Reserva %></td>
     <td><%= collection_select(:controle, :reserva_id, @reservas, :id, :sigla ) %></td>
    <tr><td><br></td></tr>
   
     <td><%= form.label :Abertura %></td>
     <td class="col-md-9" width="100" ><%= form.datetime_select :data_abertura, id: :controle_data_abertura %></td>
    <tr><td><br></td></tr>
    <tr>
     <td><%= form.label :Status %></td>
     <td class="col-md-9" width="100" ><%= form.check_box :status, id: :controle_status %></td>
    <tr><td><br></td></tr>
    <tr>
     <td><%= form.label :Munição %></td>
     <td class="col-md-9" width="100" ><%= form.number_field :qtd_municao, id: :controle_qtd_municao %></td>
    <tr><td><br></td></tr>
    <tr>
     <td><%= form.label :Armamento %></td>
     <td class="col-md-9" width="100" ><%= form.number_field :qtd_armamento, id: :controle_qtd_armamento %></td>
    <tr><td><br></td></tr>
    <tr>
     <td><%= form.label :Acessório %></td>
     <td class="col-md-9" width="100" ><%= form.number_field :qtd_acessorio, id: :controle_qtd_acessorio %></td>
    <tr><td><br></td></tr>
</table>
  
  <div class="actions">
    <%= form.submit class: 'btn btn-success btn-sm' %>
  </div>
<% end %>