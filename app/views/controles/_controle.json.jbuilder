json.extract! controle, :id, :reserva_id, :data_fechamento, :data_abertura, :status, :qtd_municao, :qtd_armamento, :qtd_acessorio, :created_at, :updated_at
json.url controle_url(controle, format: :json)
