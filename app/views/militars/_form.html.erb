<%= form_with(model: militar, local: true) do |form| %>
  <% if militar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(militar.errors.count, "error") %> prohibited this militar from being saved:</h2>

      <ul>
      <% militar.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
<table>
    <tr>
     <td><%= form.label :nome %></td>
     <td class="col-md-9" width="100" ><%= form.text_field :nome, id: :militar_nome %></td>
    <tr><td><br></td></tr>
    <tr>
     <td><%= form.label :nome_guerra %></td>
     <td class="col-md-9" width="100" ><%= form.text_field :nome_guerra, id: :militar_nome_guerra %></td>
    <tr><td><br></td></tr>
    <tr>
     <td><%= form.label :posto %></td>
     <td class="col-md-9" width="100" ><%= form.text_field :posto, id: :militar_posto %></td>
    <tr><td><br></td></tr>
</table>
  
  <div class="actions">
    <%= form.submit class: 'btn btn-success btn-sm' %>
  </div>
<% end %>