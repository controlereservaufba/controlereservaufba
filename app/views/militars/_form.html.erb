<%= form_with(model: militar, local: true) do |form| %>
  <% if militar.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(militar.errors.count, "error") %> prohibited this militar from being saved:</h2>

      <ul>
      <% militar.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :posto %>
    <%= form.text_field :posto, id: :militar_posto %>
  </div>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome, id: :militar_nome %>
  </div>

  <div class="field">
    <%= form.label :nome_guerra %>
    <%= form.text_field :nome_guerra, id: :militar_nome_guerra %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
